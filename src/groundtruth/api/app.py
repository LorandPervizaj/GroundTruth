"""FastAPI application stub for Phase 4."""

# Phase 4: implement with FastAPI
#
# Endpoints to build:
#   GET  /api/listings          — search and filter
#   GET  /api/listings/{id}     — single listing with source links
#   GET  /api/neighborhoods     — neighborhood statistics
#   GET  /api/complexes         — complex statistics
#   GET  /api/map               — geojson for map layer
#   GET  /api/budget-calculator — what can I buy with X budget
#   GET  /api/rental-yield      — rental yield calculator

APP_NAME = "groundtruth-api"
APP_VERSION = "0.1.0"
